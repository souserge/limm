const
      TSIZE = 16,
      COLS = 45,
      ROWS = 30,
      WID = COLS*TSIZE,
      HEI = ROWS*TSIZE,


      TILES = {
        WALL: 1,
        DOOR: -1,
        VANTUS: -2
      }

      EVENTS = {
        TELEPORT: -1,
        SPAWN: {
          CRABOCOP: 1
        }
      }
